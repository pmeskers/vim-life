" This file should not have to be modified directly.

let mapleader = ","     " Use comma as leader

" For global changes, modify the vim-life repo.
source ~/.vim/init/vimrc.plugins
source ~/.vim/init/vimrc.settings

" For local changes, modify the ~/.vimrc.local
source ~/.vimrc.local
